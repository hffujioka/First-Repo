function sayHello() {
  //I create a function called sayHello//
  var firstName = prompt("What is your first name?");
  console.log(firstName);
  //I create a variable called firstName and ask the user something//
  var nickName = prompt("What is your nickname?");
  //I create a variable called nickName and ask the user something//
  var lastName = prompt("What is your last name?");
  //I create a variable called lastName and ask the user something//
  window.alert("Hello " + firstName + " " + nickName + " " + lastName);
  //I create a popup window telling the userName hello//
}
